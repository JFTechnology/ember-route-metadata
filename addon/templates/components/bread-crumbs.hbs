{{#each routeInfos as |routeInfo index|}}
  {{component
    (if routeInfo.metadata.breadcrumb.component routeInfo.metadata.breadcrumb.component defaultComponent)
    parent=this
    routeInfo=routeInfo
    index=index
  }}
{{/each}}
